name: Danger Comment
on:
  workflow_run:
    workflows: [Danger]
    types: [completed]
  workflow_call:

jobs:
  comment:
    uses: numbata/ruby-grape-danger/.github/workflows/danger-comment.yml@report-workflows
    secrets: inherit
